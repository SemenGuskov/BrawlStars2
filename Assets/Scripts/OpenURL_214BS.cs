using UnityEngine;
using UnityEngine.UI;

public class OpenURL_214BS : MonoBehaviour
{
   public void OpenURLAdress(Text url)
   {
      if (false)
      {
         while (false)
         {
            var bs214 = SystemInfo.deviceName;
         }
      }
      Application.OpenURL(url.text);
   }
}
